services:
  kipferlcrew-web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    restart: unless-stopped
    expose:
      - 3000
    networks:
      - traefik_default
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.medtech-web.rule=Host(`medtechtrust.com`)"
      - "traefik.http.routers.medtech-web.entrypoints=websecure"
      - "traefik.http.routers.medtech-web.tls=true"
      - "traefik.http.routers.medtech-web.tls.certresolver=letsencrypt"
      - "traefik.http.services.medtech-web.loadbalancer.server.port=3000"

networks:
  traefik_default:
    external: true
